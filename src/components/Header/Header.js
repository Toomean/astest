
import React from 'react';
import './Header.scss';

const header = () => {
    return (
        <header className="Header">
            <div className="Header__logo"></div>
        </header>
    );
};

export default header;
